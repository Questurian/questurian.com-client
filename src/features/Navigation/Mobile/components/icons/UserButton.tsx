'use client';

import { User } from 'lucide-react';

export default function UserButton() {
  return (
    <User
      className={`
        /* Base styles */
        w-5 h-5
        /* 280px breakpoint */
        /* 320px breakpoint */
        /* 380px breakpoint */
        /* 480px breakpoint */
        /* 550px breakpoint */
      `}
    />
  );
}
