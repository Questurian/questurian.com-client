'use client';

import { X } from 'lucide-react';

export default function CloseIcon() {
  return (
    <X
      className={`
        /* Base styles */
        w-6 h-6
        /* 280px breakpoint */
        /* 320px breakpoint */
        /* 380px breakpoint */
        /* 480px breakpoint */
        /* 550px breakpoint */
      `}
    />
  );
}
